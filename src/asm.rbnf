<assembly> := (ASM (ARGS <ATOMIC>+)? (VARS <ATOMIC>+)? <instr>+)

<instr> := (LABEL <ATOMIC>)
         | (CALL <ANY>+)
         | (ASGN <ATOMIC> <ANY>)
         | (JMP <ATOMIC>)
         | (JMPNE <ANY> <ANY> <ATOMIC>)
         | (JMPEQ <ANY> <ANY> <ATOMIC>)
         | (RETURN <ANY>)

